<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block titre %}{% endblock %}</title>
    <link rel="stylesheet" href="{{asset('Assets/style.css')}}">
    <link rel='stylesheet' href="{{ asset('node_modules/bootstrap/dist/css/bootstrap.min.css') }}">
    <script src="{{ asset('node_modules/bootstrap/dist/js/bootstrap.min.js') }}"></script>
</head>
<body>
    <header>
        <div class="logo"> <span>HOPITAL</span> RPE</div>
        <h1>{% block pageTitle %}{% endblock %}</h1>
        <ul class="menu">
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{path('user.list')}}">Dashboard</a>
            {% else %}

                <a href="{{path('affichage.id', {'id': userId})}}">Affichage</a>
                <a href="{{"/Recette"}}">Insertion</a>
                <a href="{{path('trimestre', {id: userId})}}">Trimestre</a>
                <a href="{{path('app_inter', {id: userId})}}">Inter</a>
            {% endif %}
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <li><a class="dropdown-item" href="#!">Settings</a></li>
                <li><a class="dropdown-item" href="#!">Activity Log</a></li>
                <li><hr class="dropdown-divider" /></li>
                <li><a class="dropdown-item" href="{{ "/logout" }}">Logout</a></li>
            </ul>
            <a href="{{ "/logout" }}">Déconnexion</a>
        </ul>
    </header>
    {% block body %}{% endblock %}
</body>
</html>

